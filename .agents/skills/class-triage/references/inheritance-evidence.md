# Inheritance evidence

Use the hierarchy and lifecycle sections of `full-guide.md`. Prove receivers, base construction,
storage extent, and the complete ABI bundle before adding a derived declaration or override.
