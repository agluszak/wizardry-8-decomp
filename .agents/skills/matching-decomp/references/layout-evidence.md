# Layout evidence

Use the “Proving layouts and types” section of `full-guide.md`. Establish offsets from receivers,
object size, lifecycle calls, and emitted accesses; do not infer a new wrapper from convenient casts.
