GHIDRA_INSTALL_DIR=/absolute/path/to/ghidra_12.1.2_PUBLIC
WIZ8_INPUT_DIR=/absolute/path/to/local/wizardry8-inputs

# Generated proprietary data, source oracles, and the live Ghidra project.
# Keep this outside the checkout. Give concurrent checkouts separate work
# directories; never hardlink ghidra/, because Ghidra writes it in place.
WIZ8_WORK_DIR=/absolute/path/to/generated/wizardry8-work
