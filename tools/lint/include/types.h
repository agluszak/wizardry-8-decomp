#pragma once

/* The vendored SGP tree was authored for Windows' case-insensitive include
   lookup.  Preserve the upstream spelling while routing native Clang to the
   canonical tracked header. */
#include <Types.h>
