name: Apply source-model cleanup

on:
  push:
    branches:
      - fix/source-model-type-boundaries

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo runner-ok
